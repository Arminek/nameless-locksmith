# nameless-locksmith UI strings — English.
#
# Format: `key = value`. Lines starting with '#' and blank lines are ignored;
# the first '=' splits key from value, and surrounding whitespace is trimmed.
#
# To add a language:
#   1. Copy this file to src/i18n/<code>.txt and translate the values.
#   2. Register it in the LANGUAGES table near the top of src/tui.rs.
# Missing keys fall back to English, so a partial translation still works.

tab.browse = Browse
tab.solve  = Solve
tab.step   = Step

status.browse = Browse — ↑↓ move · / filter · Enter walk · d delete · Tab solve · q quit · e edit
status.solve  = Solve — Tab/↑↓ fields · Enter solve · ^W walk · ^S save · Esc back
status.step   = Step — →/Space next · ← prev · g reset · G end · Esc back
status.filter = Filter — type to match names · Enter apply · Esc clear

filter.title       = Filter
filter.placeholder = / to filter
locks.title        = Locks
detail.title       = Detail
detail.empty       = No locks match the filter.

label.rules    = Rules
label.solution = Solution
label.steps    = steps
value.none     = (none)
value.unnamed  = unnamed
msg.cantwalk   = Can't walk
msg.saved      = Saved
msg.updated    = Updated
browse.delete  = Delete
browse.deleted = Removed
browse.edit    = Edit

solve.title          = Solve a lock
field.name           = Name
field.rule           = Rule
solve.hint           = r = plate right, l = plate left, - = nothing. Start = 6 positions 1..7.
result.title         = Result
result.empty         = Fill the form, then press Enter to solve.
solve.err_start      = Start needs 6 numbers, e.g. 5, 3, 6, 7, 2, 7
solve.err_nosolution = No solution without hitting a wall — re-check the rules/start.
solve.solved_prefix  = Solved in
solve.solved_suffix  = clicks — ^W walk, ^S save
result.solved_suffix = clicks  ·  ^W walk  ·  ^S save
solve.nothing_walk   = Nothing to walk — press Enter to solve first
solve.nothing_save   = Nothing to save — press Enter to solve first
solve.cracking      = cracking the lock
solve.tumblers      = Tumblers
solve.example       = Load example
solve.format        = Format & examples
solve.err_complex   = Lock too complex to solve in the browser — try fewer tumblers
solve.err_count     = Enter one starting position (1–7) for each tumbler
help.intro        = Each lock has <b>2–8 tumblers</b> (plates) on a <b>1–7</b> track. The goal is every plate centred on <b>4</b>.
help.rule        = A <b>rule</b> describes what happens to the <i>other</i> plates when you press <b>[D]</b> on that tumbler:
help.dirs        = <code>r</code> = that plate slides <b>right</b> · <code>l</code> = slides <b>left</b> · <code>-</code> = nothing moves
help.start        = <b>Start</b> = the current position (1–7) of each plate, left to right.
help.example        = Example (6 tumblers):
help.solves        = solves in 52 clicks
help.output        = <b>Output:</b> <code>4: 2x D</code> means press <b>[D]</b> twice on tumbler 4. [A] is the opposite of [D].
help.tip        = <i>Tip:</i> if a plate is jammed at a wall (1 or 7), a [D] press can be blocked and hide a dependency — nudge plates toward the middle first.

step.lock    = Lock
step.steps   = Steps
step.current = Current move
step.tumbler = tumbler
step.goal    = align every pin on hole 4
step.open    = ✓ OPEN — all pins centered on hole 4
step.click   = click
step.pins    = pins on 4
